#include <iostream>

using namespace std;

int main() {
    double height;
    double base;

    cin >> height;
    cin >> base;

    double ans = (base * height) / 2;
    cout << ans << endl;
    return 0;
}
